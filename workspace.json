{"$defs":{"Access":{"description":"Whether a rule grants or revokes access.","oneOf":[{"const":"allow","description":"Permit the matching operation.","type":"string"},{"const":"deny","description":"Refuse the matching operation.","type":"string"}]},"AllOrKeyed":{"anyOf":[{"description":"A single value applied uniformly to every key.","type":"boolean"},{"additionalProperties":{"type":"boolean"},"description":"Per-key values. A key absent from the map has no value.","type":"object"}],"description":"A configuration value expressed either uniformly for every key or per key.\n\nDeserializes from either a single value (applied to *all* keys) or a map of\nper-key values, mirroring [`SingleOrMany`](crate::SingleOrMany) for the\n\"blanket-or-keyed\" shape:\n\n```yaml\n# blanket form — one value governs every key\nfeature: true\n\n# keyed form — a value per key\nfeature:\n  a: true\n  b: false\n```"},"CapabilitiesStrictness":{"description":"How strictly to treat a **capability floor gap** — a governed domain that,\non the resolved runtime/platform, ends up enforced only by a *bypassable*\nin-process mechanism (the RPC broker or the script shim) with no\nun-bypassable runtime-flag or OS-sandbox floor. Examples: `net`/`process` on\nBun (no permission model), or any governed domain off-Linux while no\nOS-sandbox backend covers it.\n\nThe variants are ordered `Warn < RequireFloor`, so `strictness` can be\ncombined across configuration levels by taking the [`max`](Ord::max) — the\nmost-severe stance any enclosing level declared wins.","oneOf":[{"const":"warn","description":"Proceed, surfacing each floor gap as a non-fatal warning. Enforcement is\nstill active as defense in depth. This is the shipped default.","type":"string"},{"const":"require-floor","description":"Refuse to run when any governed domain lacks an un-bypassable floor for\nthe resolved runtime/platform: a run proceeds only when every governed\ndomain is floored (so the confinement cannot be bypassed by raw sockets,\ndirect syscalls, or FFI/N-API/WASM).","type":"string"}]},"CapabilityDomain":{"description":"An abstract capability domain — the universal vocabulary shared by every\nsubsystem and every enforcement backend.","oneOf":[{"const":"fs.read","description":"Reading files / directories.","type":"string"},{"const":"fs.write","description":"Creating / writing / removing files and directories.","type":"string"},{"const":"net","description":"Outbound network access (matched as `host:port`).","type":"string"},{"const":"env","description":"Reading environment variables.","type":"string"},{"const":"process","description":"Spawning child processes.","type":"string"}]},"CapabilityPolicyConfig_workspace":{"additionalProperties":false,"default":{},"description":"A level's capability policy: an ordered list of allow/deny rules plus a floor-gap strictness stance.","properties":{"rules":{"$ref":"#/$defs/CapabilityRules_workspace"},"strictness":{"$ref":"#/$defs/CapabilitiesStrictness"}},"type":"object"},"CapabilityRules_workspace":{"default":[],"description":"An ordered list of allow/deny capability rules. Levels cascade by concatenation; a matching deny always wins.","items":{"$ref":"#/$defs/Capability_workspace"},"type":"array"},"Capability_workspace":{"allOf":[{"description":"The subsystem-neutral body of a single capability entry.\n\n`patterns` are platform-neutral: filesystem globs (`@workspace/**`),\n`host:port` for [`Net`](CapabilityDomain::Net), or plain names/globs for\n[`Env`](CapabilityDomain::Env) / [`Process`](CapabilityDomain::Process).","properties":{"access":{"$ref":"#/$defs/Access","description":"Whether this rule grants (`allow`) or revokes (`deny`) access."},"applies_to":{"$ref":"#/$defs/WorkspaceScope"},"direct":{"description":"Lower this specific pattern to a scoped OS grant so the runtime can read\nor `import()` it directly, without brokering every access. Defaults to\n`false`, in which case the pattern stays broker-mediated (and, on\nplatforms without a kernel filesystem floor, is surfaced as a floor gap\nrather than lowered). Only meaningful for filesystem-read rules.","type":"boolean"},"domain":{"description":"The capability domain this rule governs.","enum":["fs.read","fs.write","net","env","process"]},"on_unenforceable":{"anyOf":[{"$ref":"#/$defs/UnenforceablePolicy"},{"type":"null"}],"description":"What to do if this rule cannot be faithfully enforced on the current\nplatform. `None` inherits the caller's default (the fail-closed `deny`)."},"patterns":{"description":"Neutral patterns matched against the request (fs globs, `host:port`, or\nnames). An empty list matches nothing.","items":{"type":"string"},"type":"array"}},"required":["access","domain"],"type":"object"},{"$ref":"#/$defs/NoExtra"}]},"ExperimentalFeatures":{"$ref":"#/$defs/AllOrKeyed","description":"Opt-in switch for experimental / in-progress features.\n\nA transparent wrapper over [`AllOrKeyed<bool>`], so it deserializes from\neither a bare boolean (enable/disable *every* feature) or a per-feature map:\n\n```yaml\nenable_experimental: true          # every experimental feature\n# or\nenable_experimental:\n  capabilities: true               # only the named feature(s)\n```\n\nOff by default. Experimental features are not covered by stability\nguarantees and may change or be removed without notice."},"GeneratorSourceConfiguration":{"oneOf":[{"additionalProperties":false,"properties":{"path":{"$ref":"#/$defs/SingleOrMany"},"source":{"const":"local","type":"string"}},"required":["source","path"],"type":"object"},{"additionalProperties":false,"properties":{"rev":{"type":"string"},"source":{"const":"git","type":"string"},"uri":{"format":"uri","type":"string"}},"required":["source","uri","rev"],"type":"object"}]},"NoExtra":{"description":"A flattenable empty extras type.\n\nWe deliberately do *not* use `()` for the flattened extra slots: serde's\n`#[serde(flatten)]` on a unit type fails to deserialize from a map, whereas\nan empty braced struct flattens cleanly (and ignores unknown keys).","type":"object"},"SingleOrMany":{"anyOf":[{"type":"string"},{"items":{"type":"string"},"type":"array"}]},"Subsystem":{"description":"A subsystem whose scripts a workspace capability rule can govern.","oneOf":[{"const":"generator","description":"The generator subsystem.","type":"string"},{"const":"tools","description":"The tools subsystem (declared for tagging; not yet implemented).","type":"string"}]},"SubsystemScalar":{"description":"The scalar form of a [`SubsystemSelector`]: the `all` wildcard or a single\nsubsystem.","oneOf":[{"enum":["generator","tools"],"type":"string"},{"const":"all","description":"Every subsystem — those present today **and any added in the future**.\nA live wildcard, not a snapshot of the current enumeration, so a new\nsubsystem automatically inherits `all`-tagged workspace rules.","type":"string"}]},"SubsystemSelector":{"anyOf":[{"$ref":"#/$defs/SubsystemScalar","description":"`\"all\"` | `\"generator\"` | `\"tools\"`."},{"description":"`[\"generator\", \"tools\"]`.","items":{"$ref":"#/$defs/Subsystem"},"type":"array"}],"description":"Which subsystems a workspace capability rule applies to.\n\nAccepts `\"all\"`, a single subsystem (`\"tools\"`), or a list\n(`[\"generator\", \"tools\"]`)."},"ToolSourceConfiguration":{"description":"A registered source of tool manifests, mirroring\n[`GeneratorSourceConfiguration`](crate::GeneratorSourceConfiguration).\n\nThe serialized shape is intentionally identical to the generator source so\nthe two can be unified behind one shared type in a future revision without\na wire-format change.","oneOf":[{"additionalProperties":false,"properties":{"path":{"$ref":"#/$defs/SingleOrMany"},"source":{"const":"local","type":"string"}},"required":["source","path"],"type":"object"},{"additionalProperties":false,"properties":{"rev":{"type":"string"},"source":{"const":"git","type":"string"},"uri":{"format":"uri","type":"string"}},"required":["source","uri","rev"],"type":"object"}]},"Ui":{"oneOf":[{"enum":["stream","tui"],"type":"string"},{"const":"auto","description":"Automatically selects between `stream` and `tui` if there is an interactive\ntask in the execution plan.","type":"string"},{"const":"progress","description":"Renders a live progress bar with per-task spinners, capturing task output\nto disk and replaying it according to the output-logs policy.","type":"string"}]},"UnenforceablePolicy":{"description":"What to do when a rule's pattern turns out to be **genuinely unenforceable**\n— i.e. no selected enforcement backend (pre-spawn flags, OS sandbox, or\nin-process broker) can faithfully confine it on the current platform.\n\nThis is a *policy* decision owned by the author of the rule, not by any\nparticular enforcement mechanism, which is why it lives in the core model\neven though the core itself never acts on it: the enforcement layer reads it\nwhen composing a plan (see `omni_capability_enforcement`).\n\nVariants are ordered by severity (`Allow` < `Warn` < `Deny`) so that when the\nsame pattern is governed by several rules, the most severe choice wins.","oneOf":[{"const":"allow","description":"Proceed silently, dropping the unenforceable pattern (least confinement).","type":"string"},{"const":"warn","description":"Proceed, but surface a warning for the pattern that will not be enforced.","type":"string"},{"const":"deny","description":"Refuse to run and report the unenforceable pattern. **Default** — if we\ncannot confine it, we do not run it.","type":"string"}]},"WorkspaceEnvConfiguration":{"additionalProperties":false,"properties":{"files":{"default":[".env",".env.local",".env.{ENV}",".env.{ENV}.local"],"items":{"type":"string"},"type":"array"},"vars":{"additionalProperties":{"type":"string"},"default":{},"type":"object"}},"type":"object"},"WorkspaceScope":{"description":"`applies_to` selector for the [`Workspace`] capability profile.","properties":{"subsystem":{"$ref":"#/$defs/SubsystemSelector","default":"all","description":"Which subsystems this entry governs. Absent = `all`."}},"type":"object"}},"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"description":"This is the configuration file for a workspace.\nIt is used to configure the workspace and its projects.","properties":{"capabilities":{"$ref":"#/$defs/CapabilityPolicyConfig_workspace","default":{"rules":[],"strictness":"warn"},"description":"Workspace-level capability floor, applied to every run of each\nsubsystem. `rules` is a single subsystem-tagged list: each rule's\n`applies_to.subsystem` selects which subsystem(s) it governs (defaulting\nto `all`). Because evaluation is deny-dominant, a workspace `deny` can\nnever be re-opened by a generator- or action-level `allow`: lower levels\nmay only narrow this floor. This is what makes confinement mandatory by\ndefault rather than opt-in per generator. `strictness` sets the baseline\nfloor-gap stance combined most-severe with each generator/action."},"enable_experimental":{"$ref":"#/$defs/ExperimentalFeatures","default":false,"description":"Opts this workspace into experimental / in-progress features.\n\nAccepts either a bare boolean (enable or disable *every* experimental\nfeature) or a per-feature map that toggles features by name:\n\n```yaml\nenable_experimental: true          # all experimental features\n# or\nenable_experimental:\n  capabilities: true               # just the named feature(s)\n```\n\nOff by default. Currently the only feature is **capabilities**\n(capability-based sandboxing and enforcement of generator scripts): when\ndisabled, declared capabilities are still parsed and validated but not\nenforced, and generator scripts run unconfined; when enabled, the\ndeclared capability policy is enforced."},"env":{"$ref":"#/$defs/WorkspaceEnvConfiguration","default":{"files":[".env",".env.local",".env.{ENV}",".env.{ENV}.local"],"vars":{}}},"generators":{"default":[],"items":{"$ref":"#/$defs/GeneratorSourceConfiguration"},"type":"array"},"name":{"pattern":"\"\"[/\\.\\@\\:\\w\\-]+\"\"","type":["string","null"]},"projects":{"items":{"type":"string"},"type":"array"},"tools":{"default":[],"description":"Registered sources of tool manifests, mirroring `generators`. Supports\n`local` and `git` sources; discovery globs each source for\n`tool.omni.{yaml,yml,json,toml}` manifests.","items":{"$ref":"#/$defs/ToolSourceConfiguration"},"type":"array"},"ui":{"$ref":"#/$defs/Ui","default":"auto"}},"required":["projects"],"title":"Workspace Configuration","type":"object"}
